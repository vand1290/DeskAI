{
  "name": "deskai",
  "version": "1.0.0",
  "description": "Your professional helpdesk with persistent memory",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "build:backend": "tsc",
    "build:frontend": "vite build",
    "build:examples": "tsc -p tsconfig.examples.json",
    "dev": "concurrently \"tsc -w\" \"vite\"",
    "test": "vitest",
    "lint": "eslint . --ext .ts,.tsx",
    "start": "node dist/index.js",
    "example:basic": "npm run build:backend && npm run build:examples && node dist-examples/basic-usage.js",
    "example:router": "npm run build:backend && npm run build:examples && node dist-examples/router-api.js",
    "example:secretary": "npm run build:backend && npm run build:examples && node dist-examples/examples/secretary-tools.js"
  },
  "keywords": ["helpdesk", "ai", "offline", "memory"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "concurrently": "^8.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0",
    "vitest": "^1.0.0"
  }
}
